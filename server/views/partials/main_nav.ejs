 <!-- 
  File name: main_nav.ejs
  Student’s Name: Wonsuk Cha
  StudentID: 301155132
  Date: 2021.02.13.
 -->    
 
 
  <!-- Main Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <img class="nav-logo" src="/Assets/images/logo.png">
        Portfolio - Wonsuk Cha
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto mb-2 mb-lg-0">

          <% if(title=='Home' ) { %>
            <li class="nav-item active">
              <% } else { %>
            <li class="nav-item">
              <% } %>
                <a class="nav-link" aria-current="page" href="/home">
                  <i class="fas fa-laptop-house"></i> Home
                </a>
            </li>

            <% if(title=='About Me' ) { %>
              <li class="nav-item active">
                <% } else { %>
              <li class="nav-item">
                <% } %>
                  <a class="nav-link" aria-current="page" href="/about">
                    <i class="fas fa-fingerprint"></i> About Me
                  </a>
              </li>

              <% if(title=='Projects' ) { %>
                <li class="nav-item active">
                  <% } else { %>
                <li class="nav-item">
                  <% } %>
                    <a class="nav-link" aria-current="page" href="/projects">
                      <i class="fas fa-tasks"></i> Projects
                    </a>
                </li>

                <% if(title=='Services' ) { %>
                  <li class="nav-item active">
                    <% } else { %>
                  <li class="nav-item">
                    <% } %>
                      <a class="nav-link" aria-current="page" href="/services">
                        <i class="fas fa-cogs"></i> Services
                      </a>
                  </li>

                  <% if(title=='Contact Me' ) { %>
                    <li class="nav-item active">
                      <% } else { %>
                    <li class="nav-item">
                      <% } %>
                        <a class="nav-link" aria-current="page" href="/contact">
                          <i class="fas fa-envelope"></i> Contact Me
                        </a>
                    </li>

                  <% if(title=='Business Contact Edit') { %>
                    <li class="nav-item active">
                    <% } else { %>
                    <li class="nav-item">
                    <% } %>
                    <a class="nav-link" aria-current="page" href="/contact-list">
                      <i class="fas fa-address-card"></i> Business Contact List
                    </a>

                    <% if(typeof username=='string' && username) { %>
                      <li class="nav-item">
                        <a href="" class="nav-link" aria-current="page">
                          <i class="fas fa-user"> </i>
                          <%= username %>
                        </a>
                      </li>
                      <li class="nav-item"><a href="/logout" class="nav-link" aria-current="page">
                          <i class="fas fa-sign-out-alt"></i> Logout</a>
                      </li>
                      <% } else { %>
                        <li class="nav-item"><a href="/login" class="nav-link"><i class="fas fa-sign-in-alt"></i>
                            Login</a>
                        </li>
                        <% } %>

        </ul>
      </div>
    </div>
  </nav>